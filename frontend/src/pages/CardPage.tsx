// En tu ruta o componente principal
import { CardFeature } from '../features/card/components/CardFeature';
import { PageContainer } from '../components/ui/PageContainer';

export const CardUserPage = () => {
  return (
    <PageContainer>
      <CardFeature />
    </PageContainer>
  );
};