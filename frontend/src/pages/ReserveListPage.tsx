import { PageContainer } from '../components/ui/PageContainer';
import { AccountReservesContainer } from '../features/reserve/components/AccountReservesContainer';

export const ReserveListPage = () => {
  return (
    <PageContainer>
      <AccountReservesContainer />
    </PageContainer>
  );
};
